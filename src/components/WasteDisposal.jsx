import { cycles } from "../../cycles";
import { SectionTitle } from "./SectionTitle";

export default function WasteDisposal() {
    return (
        <>
            <SectionTitle>
                {cycles[6].name}
            </SectionTitle> 
        </>
    )
}