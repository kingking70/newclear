import { cycles } from "../../cycles";
import { SectionTitle } from "./SectionTitle";

export default function FuelFabrication() {
    return (
        <>
            <SectionTitle>
                {cycles[3].name}
            </SectionTitle> 
        </>
    )
}