import { cycles } from "../../cycles";
import { SectionTitle } from "./SectionTitle";

export default function FuelStorage() {
    return (
        <>
            <SectionTitle>
                {cycles[5].name}
            </SectionTitle> 
        </>
    )
}