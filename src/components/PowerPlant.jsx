import { cycles } from "../../cycles";
import { SectionTitle } from "./SectionTitle";

export default function PowerPlant() {
    return (
        <>
            <SectionTitle>
                {cycles[4].name}
            </SectionTitle> 
        </>
    )
}